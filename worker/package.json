{
  "name": "worker",
  "version": "1.0.0",
  "description": "Tiny proxy that lets the static GitHub Pages site call DeepSeek's chat API without exposing your API key in the browser.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
