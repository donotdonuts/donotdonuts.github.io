name = "leon-chat"
main = "worker.js"
compatibility_date = "2025-01-01"

[vars]
ALLOWED_ORIGIN = "https://donotdonuts.github.io,http://localhost:8000,http://127.0.0.1:8000,http://localhost:5173,http://127.0.0.1:5173"
MODEL = "deepseek-chat"

# DEEPSEEK_API_KEY is a secret — set it with:
#   wrangler secret put DEEPSEEK_API_KEY
